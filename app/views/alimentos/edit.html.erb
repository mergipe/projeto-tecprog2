<div class="d-flex justify-content-between align-items-center">
  <div>
    <h1>Editar alimento</h1>
  </div>
  <div>
    <%= button_to 'Remover', alimento_path(@alimento), method: :delete,
      data: { confirm: 'Tem certeza que deseja remover esse alimento?' },
      class: "btn btn-danger" %>
  </div>
</div>

<%= render 'form' %>
