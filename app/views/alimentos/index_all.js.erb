$('#modal').html("<%= j render 'index_all' %>");
$('#tab-content').html("<%= j render partial: 'alimentos' %>");

showLargeModal();

$(function() {
  $('#alimentos_search input').on('paste keyup', function(e) {
    $.get(
      $('#alimentos_search').attr('action'),
      $('#alimentos_search').serialize(),
      null,
      'script'
    );
  });
});
